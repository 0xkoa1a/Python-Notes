[project]
name = "python-notes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
