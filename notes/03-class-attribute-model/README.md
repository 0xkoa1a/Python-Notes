# 类与属性模型

这一层的目标是理解 Python 的类、实例、属性查找、方法绑定和继承模型。

后续笔记建议覆盖：

1. 类对象与实例对象
2. 类属性与实例属性
3. 方法绑定与 `self`
4. `property`
5. 继承、MRO 与多重继承
6. `dataclass`
7. `slots`
8. `abc` 与 `typing.Protocol`

核心信念：Python 的类首先是运行时对象，其次才是类型模板。
